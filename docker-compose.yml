version: '3.7'

services:
  pluck:
    build: .
    image: pluck
    entrypoint: bundle exec bin/pluck
    volumes:
      - ./:/usr/src/app

  dev:
    build: .
    image: pluck
    volumes:
      - ./:/usr/src/app

  test:
    build: .
    command: bundle exec rake
